# CLI Wrapper

Primary implementation lives in `../scripts/cli.sh`.

This directory provides a stable wrapper entrypoint:

- `./cli/cli.sh ...`

The wrapper forwards all arguments to `scripts/cli.sh`.
